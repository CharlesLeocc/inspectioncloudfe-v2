一.调研背景

inspectioncloudfe-v2 当前采用 Vue 3、Vite 8、JavaScript 和 Three.js 构建前端基础框架，项目已将 Three.js 精确锁定为 `0.184.0`，即 r184。V2 仍处于前期框架与技术调研阶段，需要在正式开展 3D 业务开发前，确认较新版本 Three.js 与当前工程技术栈的兼容性，并验证其基础图形、纹理、交互和资源生命周期能力是否能够作为后续开发底座。

本次调研不以复刻 V1 全部业务流程为目标，而是将 V1 中依赖 Three.js 的能力下沉为可独立验证的基础技术项。例如，病害绘制只验证鼠标拾取和点、线、面绘制能力，不复刻 V1 的 `drawComplete` 数据结构、业务校验和编辑流程；裁切只验证标准 Mesh 的裁切能力，不复刻多裁切面的完整管理流程。模型加载和 3D Tiles 已交由其他专项负责人验证，不纳入本次结论。

Three.js 版本选型不能只以版本号较新作为依据。若继续使用旧版本，可能无法获得较新的 API、兼容性改进和维护能力；若直接追随最新版本，又会增加 API 变化、示例差异和第三方生态适配风险。因此，本次需要通过实际 Demo 验证 r184 的核心能力、资源释放和异常恢复表现，并在明确验证范围和已知限制的前提下，判断其是否具备成为 V2 当前基线版本的条件。

二.调研方案

方案一：继续沿用 V1 使用的旧版 Three.js

设计模式：采用低变更版本复用模式，沿用 V1 已使用过的 Three.js 版本和既有实现经验，V2 仅完成 Vue 3 与 Vite 环境下的工程接入。

设计思路：优先降低版本升级带来的 API 调整成本，复用旧版功能实现、问题规避方式和团队已有经验，不主动引入新版本能力。

核心原理：通过保持 Three.js 主版本基线不变，减少材质、加载器、渲染器、示例扩展和自定义 Shader 适配差异，将迁移风险集中在业务框架变化上。

使用场景：V2 需要与 V1 保持完全一致、开发周期极短，且旧版仍满足浏览器兼容、安全维护和新增业务要求的项目。

方案二：采用 Three.js r184 并锁定精确版本

设计模式：采用稳定基线加专项验证模式，将 `three@0.184.0` 作为固定依赖，通过独立验证页面拆分基础图元、图片纹理、纹理投影、裁切、拾取绘制和资源监控六个技术节点。

设计思路：不提前迁移完整 V1 业务逻辑，而是先验证业务依赖的底层 Three.js 能力。每个节点只验证关键技术链路，同时覆盖 RAF 管理、GPU 资源释放、异步结果隔离和 WebGL 上下文恢复等生命周期要求。后续真实业务按需接入，并在模型、3D Tiles 等专项完成后合并生产结论。

核心原理：利用 r184 的标准 Scene、Camera、Geometry、Material、Texture、Raycaster、Clipping Plane 和 WebGLRenderer API 建立基础能力基线；通过 `renderer.info` 观测 Geometry、Texture、Program、Draw Call 和 Triangle 等指标，并通过重复创建与释放验证资源数量是否回归基线。

使用场景：需要使用较新 Three.js 能力，同时希望控制升级风险、按阶段推进 V2 框架和业务建设，并能够对每次后续升级重复执行核心回归的项目。

方案三：直接采用 Three.js 最新发布版本并持续跟随升级

设计模式：采用滚动版本升级模式，始终选择当前最新发布版本，依赖最新官方 API、示例和修复，不长期固定在单一版本。

设计思路：通过持续升级快速获取新特性和兼容性修复，并同步调整业务代码、第三方库和自定义渲染实现。

核心原理：以 Three.js 发布节奏作为版本推进依据，每次版本变化后重新验证渲染、材质、加载、交互、Shader、插件和构建产物，及时消化破坏性变化。

使用场景：具备持续兼容性投入、自动化视觉回归和完整跨浏览器测试能力，并且业务确实需要最新版本特性的研发型项目。

三.方案对比

r137 与 r184 中和 V1 功能相关的主要变更如下。表中“r184”表示 V2 应采用的目标写法或目标行为；未列出的中间版本变化不作为 V1 功能迁移依据。

| 功能或机制                   | r137 中的 API 或行为                                                                             | r184 中的 API 或行为                                                                            | 对 V1 功能的影响与处理                                                                                                   |
| ---------------------------- | ------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| Renderer 输出颜色            | `renderer.outputEncoding`，默认 `LinearEncoding`                                                 | `renderer.outputColorSpace`，默认 `SRGBColorSpace`                                              | 模型、图片、标注颜色和截图明暗可能变化；V2 必须明确输出颜色空间并进行视觉基线对比                                        |
| 纹理颜色空间                 | `texture.encoding`                                                                               | `texture.colorSpace`                                                                            | 颜色纹理设置为 `SRGBColorSpace`；法线、深度、粗糙度等数据纹理保持 `NoColorSpace`                                         |
| 颜色空间常量                 | `sRGBEncoding`、`LinearEncoding`                                                                 | `SRGBColorSpace`、`LinearSRGBColorSpace`；无颜色空间数据使用 `NoColorSpace`                     | V1 私有 GLTFLoader、KTX2Loader 仍导入旧常量，不能直接绑定 r184，必须换成同版本 addons 和转码器                           |
| 颜色管理默认行为             | 未默认启用 r152 后的新颜色管理流程，应用通常自行处理线性和 sRGB 转换                             | `ColorManagement.enabled = true`，颜色输入在工作颜色空间中统一转换                              | CSS 色值、材质颜色、CanvasTexture 和图片纹理需要按用途声明颜色空间，避免重复或遗漏转换                                   |
| 灯光模式                     | `renderer.physicallyCorrectLights = false`，默认使用旧灯光模型                                   | 物理正确灯光已成为标准行为，不再依赖旧兼容开关                                                  | 相同 intensity 下的模型亮度和受光范围可能变化，需要重新标定灯光参数，不能直接沿用 V1 数值                                |
| PointLight、SpotLight 衰减   | 构造函数默认 `decay = 1`                                                                         | 构造函数默认 `decay = 2`                                                                        | V1 创建点光源和聚光灯时未显式传入 decay，迁移后照射距离和亮度分布会发生变化                                              |
| Lambert 和 PBR 材质          | `MeshLambertMaterial` 使用旧版着色实现；PBR、PMREM 为 r137 计算结果                              | Lambert 改为逐片元着色；r181 后 PBR 能量守恒和 PMREM 反射进一步改进                             | 材质 API 仍可用，但模型粗糙度、反射、亮度和阴影观感需要视觉回归                                                          |
| 多重采样 RenderTarget        | `WebGLMultisampleRenderTarget`                                                                   | `WebGLRenderTarget` 或其他 RenderTarget 的 `samples` 属性                                       | 旧多重采样目标不能原样迁移；截图和后处理目标应改为 `renderTarget.samples = n`                                            |
| 后处理颜色输出               | Tone Mapping 和编码通常由 Renderer 或旧 Composer 链路直接处理                                    | RenderTarget 阶段保持工作颜色空间；后处理末端使用 `OutputPass` 完成 Tone Mapping 和颜色空间转换 | V1 的 Bloom、FXAA、亮度、对比度、锐化和截图链路需重新组装，防止漏转换或重复转换                                          |
| EffectComposer 默认目标      | 默认使用普通字节类型 RenderTarget                                                                | r153 后默认使用 `HalfFloatType` RenderTarget                                                    | 后处理精度更高，但显存占用和低端 GPU 兼容性需要验证；截图结果也需重新对比                                                |
| Shader 颜色输出 Chunk        | `#include <encodings_fragment>`                                                                  | `#include <colorspace_fragment>`                                                                | V1 私有 LineMaterial 和 Sky 仍使用旧 Chunk，绑定 r184 会导致 Shader 编译失败，应改用 r184 addons 或更新自定义 Shader     |
| Shader 最终输出 Chunk        | `#include <output_fragment>`                                                                     | `#include <opaque_fragment>`                                                                    | 复用 V1 自定义材质、投影和后处理 Shader 时必须逐项替换并验证透明度、Tone Mapping 和颜色输出                              |
| Addons 引入方式              | 项目内复制 `examples/jsm` 文件，或直接引用旧版 GLTFLoader、KTX2Loader、OrbitControls、Line2、Sky | 使用与核心版本一致的 `three/addons/...` ES Module                                               | V2 不应复制 V1 的 `threeApi/jsm`；宽线、控制器、天空和 Loader 必须跟随 r184 一起升级                                     |
| 几何体类命名                 | 可使用 `BoxBufferGeometry`、`PlaneBufferGeometry` 等 Buffer 别名                                 | 使用 `BoxGeometry`、`PlaneGeometry` 等正式名称                                                  | V1 业务主要已使用 BufferGeometry；迁移时清理旧别名，避免依赖已移除的兼容导出                                             |
| BufferGeometryUtils 合并方法 | `mergeBufferGeometries()`、`mergeBufferAttributes()`                                             | `mergeGeometries()`、`mergeAttributes()`                                                        | 如果迁移 V1 几何合并工具，需要修改方法名，并重新确认 groups、index 和 userData 处理                                      |
| BufferAttribute 局部更新     | 单个 `attribute.updateRange`                                                                     | `attribute.updateRanges`，通过 `addUpdateRange()` 和 `clearUpdateRanges()` 管理                 | 动态线、点和频繁更新的 Geometry 若使用局部上传，需要迁移到多区间更新 API；仅设置 `needsUpdate` 的逻辑仍可使用            |
| InstancedMesh 视锥裁剪       | `InstancedMesh.frustumCulled = false`                                                            | 默认继承 `frustumCulled = true`，支持实例包围体计算                                             | V1 实例视锥和批量标记在 `setMatrixAt()` 后必须调用 `computeBoundingSphere()`，否则移动后可能错误消失或拾取不到           |
| Mipmap 生成条件              | Mipmap 生成行为会受到纹理过滤配置影响                                                            | 只要 `texture.generateMipmaps = true` 就会生成，不再依赖过滤器是否选择 Mipmap                   | V2 必须将 `minFilter` 采样方式和 `generateMipmaps` 生成开关作为两个独立参数管理                                          |
| WebGL 运行环境               | WebGLRenderer 优先 WebGL2，并可回退 WebGL1                                                       | r163 起 WebGLRenderer 不再支持 WebGL1，只使用 WebGL2                                            | V2 需将 WebGL2 作为运行门槛；V1 中直接调用 `getContext('webgl')` 的清理逻辑应改用 Renderer 上下文或 `forceContextLoss()` |
| Stencil 默认值               | Renderer 上下文默认启用 stencil                                                                  | Renderer 的 `stencil` 默认值为 `false`                                                          | 如果后处理实际使用 MaskPass 或自定义模板缓冲，创建 Renderer 时必须显式设置 `stencil: true`；未使用则无需开启             |
| 动画计时                     | 使用 `Clock`                                                                                     | r184 中 `Clock` 仍可用但从 r183 起已弃用，推荐使用 `Timer`                                      | 当前动画循环不会立即失效；V2 新代码使用 Timer，旧 Clock 逻辑在业务迁移时替换                                             |
| Sky 外观                     | 旧 Sky 包含历史 Gamma 修正                                                                       | r183 移除 Sky 的历史 Gamma 修正                                                                 | 天空颜色和亮度无法仅通过旧参数保证完全一致，应以 V2 视觉基线重新调参                                                     |
| DepthTexture 默认类型        | `DepthFormat` 下默认使用 `UnsignedShortType`                                                     | r141 后默认使用 `UnsignedIntType`                                                               | V1 图片投影和深度采样使用 `new DepthTexture()`，迁移后需要验证深度精度、Shader 采样和目标 GPU 支持情况                   |

各验证节点结果如下：

| 验证节点 | 验证内容                                                                                                                                                                                     | 验证结果 | 结论边界                                                                                |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------- |
| 基础图元 | 透视与正交相机；Point、Line、Line2 宽线、面、Mesh、Sprite；基础、光照和 PBR 材质；灯光、透明度、Depth Test、OrbitControls、视角复位、单 RAF 暂停与恢复                                       | 通过     | 证明基础场景、图元、材质和相机能力可用，不代表 V1 业务对象已完成迁移                    |
| 图片纹理 | CanvasTexture、内置 JPEG、本地图片和大图加载；解码尺寸、耗时、内存估算、`MAX_TEXTURE_SIZE`；Plane、Box、Sphere 贴图；Wrapping、Filter、Mipmap、Anisotropy、`flipY`；旧纹理释放和异步结果隔离 | 通过     | 证明大图加载链路和纹理参数可用，不保证所有极端尺寸、格式和设备组合均能成功              |
| 纹理投影 | 单投影相机；正面、斜向和俯视方向；FOV、投影距离、启停、CameraHelper；当前图片纹理复用和投影材质更新                                                                                          | 通过     | 仅覆盖基础 Projective Texture，不包含多投影、遮挡混合、实时视频和精确标定               |
| 裁切能力 | Box、Sphere、TorusKnot；X、Y、Z 轴；裁切位置、正反方向、启停、PlaneHelper；`localClippingEnabled` 和 `clippingPlanes`                                                                        | 通过     | 仅覆盖标准 Mesh 的单裁切面，不包含 V1 多裁切面业务流程、复杂布尔裁切和裁切封口          |
| 拾取绘制 | 独立 Raycaster 拾取；命中对象、世界坐标和高亮；鼠标绘制点、线、面；完成、清空、拖动阈值、Geometry 更新释放和最大点数限制                                                                     | 通过     | 不依赖裁切面过滤；不包含 V1 病害协议、`drawComplete` 契约、吸附、撤销重做、编辑和序列化 |
| 资源监控 | `renderer.info` 指标；12 个独立测试 Mesh 创建与释放；连续 10 次循环；RAF 暂停恢复；WebGL 上下文丢失、恢复、GPU 预热和基线重建                                                                | 通过     | 能发现明显资源释放问题，但不能替代数小时长稳、数千对象或显存极限压力测试                |

资源生命周期验证中，每批测试资源的创建和释放时间复杂度为 `O(n)`、空间复杂度为 `O(n)`，当前 `n = 12`；10 次循环为 `O(10n)`。资源释放后 Geometry、Texture 和 Program 指标均回到对应基线，未观察到随循环次数持续增长。WebGL 上下文丢失时 RAF 能够停止，恢复后可重新预热并建立有效基线；全新 Chromium 会话控制台为 0 个 error、0 个 warning，`390 × 844` 视口下未发现控件重叠或横向溢出。

各选型方案对比如下：

| 对比指标             | 方案一：沿用旧版                         | 方案二：采用 r184 并锁定                           | 方案三：持续跟随最新版                      |
| -------------------- | ---------------------------------------- | -------------------------------------------------- | ------------------------------------------- |
| 与 V2 当前依赖一致性 | 需要调整当前依赖并重新确认构建兼容性     | 已与当前 `three@0.184.0` 一致                      | 每次升级均可能改变当前依赖和 API            |
| 基础能力证据         | 依赖 V1 历史经验，缺少 V2 当前环境验证   | 六个核心节点已完成实际验证                         | 当前未完成对应版本验证                      |
| 迁移成本             | 初期较低，但未来升级跨度和技术债务较大   | 当前成本可控，业务可按节点接入                     | 持续产生 API、示例和第三方生态适配成本      |
| 版本可复现性         | 可锁定，但继续依赖较旧基线               | 精确锁定 `0.184.0`，环境可复现                     | 若追随 latest，构建和问题复现容易随时间漂移 |
| 新能力与维护性       | 新 API 和后续修复获取受限                | 能使用 r184 能力，并保留后续按需升级空间           | 新能力获取最快，但稳定基线最弱              |
| 风险控制             | 旧版浏览器兼容和长期维护风险需要重新评估 | 已发现风险有明确边界，可通过专项和业务回归继续收敛 | 每次升级都需要重跑完整兼容性验证            |
| 当前阶段适配度       | 不符合使用较新稳定版本的调研目标         | 符合 V2 前期框架验证和阶段性建设要求               | 超出当前阶段所需，投入与收益不匹配          |

当前已知限制如下：

| 限制类别   | 已知限制                                                                                                                                             | 后续控制措施                                                                                              |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| 验证环境   | 当前主要基于本机 Chromium、WebGL2 和现有 GPU 驱动；未系统覆盖 Firefox、Safari、移动端真机、低端集显和多种 GPU 驱动组合                               | 在首个真实业务页面接入后补充目标浏览器、移动设备和典型 GPU 环境回归                                       |
| 压力规模   | 12 个对象、10 次循环属于资源释放冒烟验证，不能代表超大场景或长时间运行稳定性                                                                         | 增加数小时长稳、重复路由进出、数千对象和显存压力测试                                                      |
| 大图纹理   | 文件大小不能代表解码内存；RGBA 解码内存近似为 `width × height × 4`，完整 Mipmap 显存通常再增加约三分之一；超过 GPU `MAX_TEXTURE_SIZE` 时无法直接上传 | 保留运行时能力检测和指标展示；超限图片采用缩放、切片或瓦片策略，不设置任意 20 MB 或 5000 万像素通用硬限制 |
| 纹理投影   | 未覆盖多投影、遮挡处理、投影混合、视频纹理、精确相机标定和业务坐标转换                                                                               | 对实际投影业务建立独立精度、遮挡和坐标转换验证                                                            |
| 裁切       | 标准材质的 `clippingPlanes` 不代表 Gaussian Splat、自定义 Shader 或其他第三方渲染对象天然支持裁切                                                    | 第三方对象和自定义渲染管线分别验证 Shader 裁切能力及资源恢复逻辑                                          |
| 拾取绘制   | 仅验证 Raycaster 和点、线、面底层交互，不覆盖业务数据协议、精度、编辑、吸附和撤销重做                                                                | 在真实标注模块中验证业务坐标、数据契约和完整交互状态                                                      |
| 上下文恢复 | 当前覆盖基础 Three.js 场景；自定义 Shader、RenderTarget、Worker、后处理、视频纹理和第三方渲染器的重建责任尚未验证                                    | 新增对应资源时明确所有权、失效隔离、恢复和销毁责任，并执行上下文丢失回归                                  |
| 专项范围   | 模型加载、3D Tiles 和 Gaussian Splat 不在本轮验证范围内                                                                                              | 合并其他负责人专项结果后再形成完整生产采用结论                                                            |

四.总结

最终结论：选定方案二，将 `three@0.184.0` 作为 inspectioncloudfe-v2 当前阶段可采用的 Three.js 基线版本，并保持精确版本锁定。在当前 Vue 3、Vite 8、本机 Chromium、WebGL2 和现有 GPU 环境下，基础图元、相机、材质、图片纹理、大图观测、纹理投影、标准 Mesh 裁切、Raycaster 拾取、点线面绘制以及基础 GPU 资源生命周期均验证通过，未发现阻断 V2 前期框架开发和 Three.js 基础能力建设的重大缺陷。该结论不等同于 V1 全量 3D 功能迁移完成，也不代表 r184 不存在任何公开 issue 或平台兼容问题；完整生产结论需合并模型加载和 3D Tiles 专项结果，并完成真实业务、跨浏览器和长时间稳定性回归。

选型依据：r184 已与项目当前依赖一致，六个核心技术节点均有可运行 Demo 和实际验证结果，基础渲染、交互、资源释放及上下文恢复未出现阻断问题；精确锁定版本能够保证开发、测试和问题复现的一致性。相较旧版，r184 更符合 V2 使用较新稳定技术基线的目标；相较持续跟随最新版，固定 r184 能显著降低 API 漂移和第三方生态适配成本。当前已知风险具有清晰边界，可以通过模型与 3D Tiles 专项、真实业务接入、跨浏览器和长稳测试继续收敛。

不选用其他方案原因：不选择方案一，是因为沿用旧版无法满足本次使用较新稳定版本的目标，并会将浏览器兼容、维护和未来大跨度升级风险继续累积；不选择方案三，是因为直接追随最新版缺少稳定版本边界，每次升级都会引入 API、示例扩展、自定义 Shader 和第三方库重新适配成本，而当前业务尚无必须使用最新版特性的明确收益。后续仅在出现明确业务需求、安全问题或关键兼容性修复时评估升级，升级后重新执行六个核心验证节点。
