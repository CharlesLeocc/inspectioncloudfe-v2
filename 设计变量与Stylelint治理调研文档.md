一.调研背景

当前项目采用 Vue 3、Vite、Less 和 Element Plus 构建前端样式体系。早期设计变量由前端在 Less Token 文件和明暗主题文件中手工维护，设计稿中的颜色、字体、阴影、间距等属性无法直接成为项目的唯一数据源。设计侧调整变量后，需要前端人工识别差异并同步修改多份文件，容易出现变量遗漏、名称不一致、主题间变量集合不一致以及设计稿与代码长期偏离的问题。

业务样式中同时存在 CSS 变量、Less 变量和硬编码色值。原生 Stylelint 可以检查颜色格式，但无法判断某个硬编码色值是否已经存在对应的 MasterGo 设计变量，也无法根据项目变量自动给出替换建议。仅依赖代码评审发现硬编码颜色，反馈时间晚且检查结果受评审人员经验影响，不能形成稳定的工程约束。

项目中的样式文件包含 CSS、Less 和 Vue 单文件组件三种载体。Stylelint 命令行可以通过 `postcss-less` 和 `postcss-html` 解析 Less 与 Vue 样式，但编辑器扩展默认只校验 CSS 和 PostCSS。若未显式配置 `stylelint.validate`，就会出现普通 CSS 文件能够实时提示，而 `reset.less` 和 `App.vue` 中同类硬编码颜色没有实时提示的现象。编辑器、命令行和 Git 提交阶段的覆盖范围不一致，会使开发人员误判规则是否生效。

MasterGo 导出的变量 CSS 是设计源产物，必须保留原始颜色值。若将业务代码使用的硬编码颜色规则直接应用到变量定义文件，自动修复可能把 `--变量名: #ff5252` 改为 `--变量名: var(--变量名)`，形成无效的自引用。因此，设计源、自动生成文件和业务消费代码需要具有明确的职责边界和不同的校验策略。

本次调研需要确定一套适用于当前技术栈的设计变量治理方案，使 MasterGo 成为设计变量来源，自动生成前端运行时变量和辅助映射，统一 CSS、Less、Vue 的硬编码颜色检查，并在开发启动、生产构建和 Git 提交阶段建立一致性门禁，降低人工同步和样式回归成本。

二.调研方案

方案一：前端手工维护 Less Token 和主题文件

设计模式：采用编译期变量集中管理模式，由前端在 `color.less`、`layout.less`、`typography.less` 及明暗主题文件中维护 Less 变量，业务样式通过 Less 变量或 Mixin 消费。

设计思路：设计人员提供变量变更说明，前端根据设计稿人工修改 Token 文件。Stylelint 只使用标准规则检查语法和颜色格式，设计变量与硬编码颜色之间的对应关系依赖人工评审。

核心原理：Less 在构建阶段将变量替换为静态值，不需要额外的同步脚本和运行时主题清单。不同主题通过独立 Less 文件、构建参数或主题类进行覆盖。

使用场景：设计变量数量较少、主题固定、设计变更频率低，且没有稳定设计变量导出能力的小型项目。

方案二：直接引入 MasterGo 导出的 CSS 变量文件

设计模式：采用设计源文件直连模式，将 MasterGo 导出的 CSS 文件直接放入项目，通过全局样式入口加载，业务样式直接使用导出的 CSS 自定义属性。

设计思路：不建立中间转换层和颜色映射表，MasterGo 导出文件同时作为设计源和运行时文件。主题通过导出文件中的 `:root` 和 `[data-theme]` 选择器实现，前端只负责加载文件和切换根节点属性。

核心原理：浏览器运行时解析 `var(--变量名)`，设计变量修改后重新覆盖导出 CSS 文件即可生效。硬编码颜色仍由标准 Stylelint 规则或代码评审处理。

使用场景：需要快速验证 MasterGo CSS 变量接入效果、变量结构稳定、团队可以严格执行手工替换，并且暂不要求自动校验生成物和硬编码颜色语义的项目。

方案三：MasterGo CSS 同步脚本与自定义 Stylelint 联合治理

设计模式：采用“设计源、生成层、消费层、质量门禁”分层模式。MasterGo CSS 是外部设计源；Node.js 同步脚本负责结构校验和生成；CSS 变量是运行时消费接口；自定义 Stylelint、编辑器配置和 Git Hook 负责质量约束。

设计思路：设计人员从 MasterGo 导出标准 CSS 文件，前端执行 `npm run tokens:sync -- <导出文件路径>`。同步脚本使用 PostCSS 解析文件，校验文件头、`:root`、`[data-theme]`、变量声明、主题变量集合、尺寸单位和变量引用，随后生成 `design-tokens.css`、`themes.js` 和 `color-map.json`。

核心原理：运行时通过 CSS 自定义属性和根节点 `data-theme` 完成主题切换；主题清单由生成脚本维护并通过 `localStorage` 持久化用户选择。自定义 Stylelint 规则扫描业务声明中的十六进制、`rgb()` 和 `rgba()` 颜色，将标准化色值与 `color-map.json` 匹配：唯一匹配时支持自动替换，多个匹配时要求按语义选择，无匹配时阻止引入未登记颜色。

编辑器通过项目级 `stylelint.validate` 将 `css`、`postcss`、`less` 和 `vue` 全部纳入实时检查；Stylelint 配置分别使用 `postcss-less` 和 `postcss-html` 解析 Less 与 Vue。MasterGo 源文件和自动生成目录从业务硬编码颜色规则中排除，避免变量定义被修改为自引用。

开发启动和生产构建前执行 `tokens:validate`，检查生成文件是否缺失或过期；Husky pre-commit 先校验 Token 一致性，再由 lint-staged 对 Vue、CSS、Less 和 JavaScript 等暂存文件分类执行 Stylelint、ESLint 和 Prettier。

使用场景：设计变量数量较多、存在主题切换、设计变更频繁，需要统一 Vue、Less、CSS 校验，并希望在不引入大型 Token 平台的前提下建立自动化治理的 Vue 3 项目。

方案四：引入 DTCG 中间格式与 Style Dictionary

设计模式：采用平台无关 Token 中间层模式。MasterGo 变量先转换为 Design Tokens Community Group 格式的 JSON，再通过 Style Dictionary 或同类工具分别生成 Web CSS、Less、JavaScript、移动端及其他平台产物。

设计思路：建立独立 Token 包或 Token 仓库，定义颜色、尺寸、字体、阴影、别名和主题的标准 Schema。各业务项目只依赖版本化 Token 包，不直接处理 MasterGo CSS。校验、转换、发布和变更记录在独立流水线中完成。

核心原理：以结构化 JSON 表达 Token 类型、值、引用和元数据，使用转换器针对不同平台输出产物。主题、别名和跨平台命名在生成阶段统一处理，业务仓库通过包版本控制升级节奏。

使用场景：同时服务多个 Web 项目、移动端或桌面端，需要跨平台复用、版本发布、变更审计和独立设计系统团队维护的大型组织。

三.方案对比

| 对比指标           | 方案一：手工维护Less Token               | 方案二：直接引入MasterGo CSS | 方案三：同步脚本与Stylelint联合治理            | 方案四：DTCG与Style Dictionary      |
| ------------------ | ---------------------------------------- | ---------------------------- | ---------------------------------------------- | ----------------------------------- |
| 设计变量来源       | 设计稿与前端文件并存，前端文件为实际来源 | MasterGo导出CSS              | MasterGo导出CSS，生成产物由脚本统一维护        | DTCG Token仓库                      |
| 设计与代码同步方式 | 人工同步                                 | 手工覆盖导出文件             | 命令同步并校验                                 | 独立流水线转换和发包                |
| 输入结构校验       | 无                                       | 无                           | 校验文件头、选择器、变量、主题集合、单位和引用 | 通过Schema和构建工具校验            |
| CSS、Less、Vue覆盖 | 依赖人工约定                             | 依赖人工约定                 | 统一Stylelint配置和对应PostCSS解析器           | 仍需在业务项目配置Stylelint         |
| 硬编码颜色治理     | 代码评审                                 | 标准Stylelint或代码评审      | 按设计变量映射自动检查                         | 需要额外开发映射规则或插件          |
| 自动修复安全性     | 不支持                                   | 不支持                       | 唯一映射自动修复，歧义和未知颜色不自动修复     | 取决于额外治理工具                  |
| 主题支持           | 多份Less文件，维护成本高                 | 支持导出的data-theme         | 自动生成主题清单并支持运行时切换               | 支持，但需设计主题Schema和转换规则  |
| 生成物一致性       | 无生成物校验                             | 无一致性校验                 | 开发、构建、提交阶段均校验                     | Token包构建与发布阶段校验           |
| 设计源隔离         | 不涉及                                   | 设计源与运行时文件耦合       | 设计源、生成文件、业务代码分层隔离             | 设计源、Token仓库、业务项目完全分离 |
| Element Plus接入   | 手工维护Less或CSS映射                    | 手工维护CSS映射              | 保留集中映射层并校验引用来源                   | 通过转换模板生成或集中维护映射      |
| 初始改造成本       | 低                                       | 低                           | 中                                             | 高                                  |
| 日常维护成本       | 高                                       | 中                           | 低                                             | 中，需要维护独立平台和发布流程      |
| 多项目及跨平台能力 | 弱                                       | 弱                           | 中，可复用脚本但仍以Web CSS为中心              | 强                                  |
| 当前项目适配度     | 低                                       | 中                           | 高                                             | 中，能力超出当前单项目需求          |
| 主要风险           | 人工遗漏和设计漂移                       | 缺少校验，导出文件容易被误改 | 需维护同步脚本、自定义规则和编辑器配置         | 工程复杂度、迁移和组织协作成本高    |

四.总结

最终结论：选用方案三“MasterGo CSS 同步脚本与自定义 Stylelint 联合治理”，作为当前项目设计变量同步、主题运行时、硬编码颜色检查和代码质量门禁的统一方案。

选型依据：当前项目是以 Vue 3、Vite、Less 和 Element Plus 为核心的单一 Web 项目，已经具备稳定的 MasterGo CSS 导出能力。方案三使用现有 Node.js、PostCSS、Stylelint、Husky 和 lint-staged 工具即可完成设计源校验、生成物管理、主题切换、硬编码颜色治理以及开发和提交门禁，不需要增加独立服务或发布平台。验证结果表明，自定义规则能够同时检查 CSS、Less 和 Vue 样式，唯一色值映射可以自动修复，歧义和未知颜色能够保留人工语义判断；通过项目级 `stylelint.validate` 可以消除编辑器语言覆盖差异。该方案在自动化程度、改造成本和长期维护成本之间最符合当前项目约束，并保留未来向 DTCG Token 平台迁移的空间。

不选用其他方案原因：方案一依赖人工同步，无法解决设计与代码漂移、主题变量遗漏和硬编码颜色持续进入业务代码的问题；方案二虽然接入成本低，但设计源与运行时文件耦合，缺少结构校验、生成物一致性校验和颜色语义治理，不能形成完整工程闭环；方案四具备最强的跨平台和多项目能力，但当前项目尚无独立 Token 包发布、多端产物和跨团队版本治理需求，引入 DTCG Schema、转换器、独立仓库及发布流水线会显著增加建设和维护成本，现阶段收益不足以覆盖复杂度。
